# Changelog

## [1.0.5] - 2026-08-17

### Added
- `BuildConfig` support for safer version access.
- New `update.json` for GitHub release system.

### Changed
- Improved `ResourceManagementDialog` UI for Simple Mode (better category layout).
- Cleaned up `Dialogs.kt` (removed unused imports, fixed redundant qualifiers).
- Updated hardcoded version fallback in Settings screen.
- Incremented `versionCode` to 5 and `versionName` to 1.0.5.

### Fixed
- Various UI warnings and potential memory leaks in Dialogs.
- Fixed version display fallback logic.
